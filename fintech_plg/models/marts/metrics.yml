version: 2

metrics:
  - name: total_revenue
    label: Total Revenue
    model: ref('mart_revenue_by_plan')
    description: "Total purchase revenue by date and plan."
    calculation_method: sum
    expression: revenue
    timestamp: event_date
    time_grains: [day, week, month]
    dimensions:
      - plan_type

